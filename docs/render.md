file in backend read using node fs

renders html template that contains chat text with opacity 0 for indexing

at -> prepares a different file with at locale

middleware checks accept language for deciding whether to render at or en

images for portfolio sections are rendered using overflowing marquee

when route like /about visited directly just show only about content with option to load other content

when page already exists and has shown data, button clicks add #section=XYZ

session storage saves all the chat bubble groups
